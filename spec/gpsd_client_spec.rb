require 'spec_helper'

describe GpsdClient do
  it 'has a version number' do
    expect(GpsdClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
